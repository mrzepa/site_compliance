unifi:
  controllers:
    controller-a:
      username: readonly-admin@example.com
      password: change-me
      # Optional. Use a base32 TOTP seed, not a one-time MFA code.
      mfa_secret:
      # Optional. If set, the API key is tried first.
      api_key:
